// not errorneous

int digit_count(int num)
{
    int count = 0 ;
    while (num)
    {
        count++;
        num /= 10;
    }
    return count;
}

int main()
{
    int num;
    input("enter the number ", num);
    output(digit_count(num));
}